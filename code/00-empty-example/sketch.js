
// "sets up" our canvas - it only happens ONCE
function setup() {
	createCanvas(x, y); //width and height IN PIXELS of my canvas
	ellipse(x/2, y/2, 100, 100); // args are x, y, w, h

}

// This "draws" our code - it happens REPEATEDLY
function draw() {

}